info:
  name: List Deliveries
  type: http
  seq: 2

http:
  method: GET
  url: "{{DELIVERY_URL}}/deliveries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
