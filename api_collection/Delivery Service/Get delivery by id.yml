info:
  name: Get delivery by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{DELIVERY_URL}}/delivery/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
