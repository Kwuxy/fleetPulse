info:
  name: Create delivery
  type: http
  seq: 1

http:
  method: POST
  url: "{{DELIVERY_URL}}/deliveries"
  body:
    type: json
    data: |-
      {
        "client_id": 42,
        "pickup_location": "Brussels",
        "dropoff_location": "Paris",
        "cargo_weight_kg": 700,
        "requested_date": "2026-08-15"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
