info:
  name: Create Truck
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/trucks"
  body:
    type: json
    data: |-
      {
        "plate_number": "AB-123-CD",
        "capacity_kg": 1200
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
