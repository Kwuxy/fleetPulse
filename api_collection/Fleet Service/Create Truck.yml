info:
  name: Create Truck
  type: http
  seq: 1

http:
  method: POST
  url: "{{FLEET_URL}}/trucks"
  body:
    type: json
    data: |-
      {
        "plate_number": "{{license_plate}}",
        "capacity_kg": 1200
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
